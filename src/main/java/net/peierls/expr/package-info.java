/**
 * Provides an extensible framework for building, evaluating, and formatting
 * double-precision expressions (constants, variables, unary and binary operations,
 * and binding of variables).
 */
package net.peierls.expr;